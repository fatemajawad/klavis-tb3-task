schema_version = "1.3"

[metadata]
name = "dbt-incremental-debugger"
description = "Debug a dbt incremental model with 3 silent SQL bugs."

[environment]
dockerfile = "environment/Dockerfile"

[verifier]
timeout_sec = 600.0
environment_mode = "shared"

[agent]
timeout_sec = 900.0
